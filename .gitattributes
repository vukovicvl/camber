# Normalize line endings: LF in the repo, native on checkout.
* text=auto

# Always LF for these (scripts / cross-platform text)
*.py   text eol=lf
*.toml text eol=lf
*.md   text eol=lf
*.iss  text eol=lf
*.spec text eol=lf

# Binary assets — never touch
*.ico  binary
*.png  binary
